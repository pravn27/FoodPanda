import React, { Component } from 'react';
import { View, Text } from 'react-native';

export default class FoodItemDetails extends Component{
    render(){
        return(
            <Text>Food item details Page</Text>
        )
    }
}